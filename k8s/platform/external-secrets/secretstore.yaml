apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: example-secretstore
  namespace: sample-app
spec:
  provider:
    kubernetes:
      remoteNamespace: sample-app
      server:
        caProvider:
          type: ConfigMap
          name: kube-root-ca.crt
          key: ca.crt
